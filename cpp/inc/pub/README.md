# Public Include Directory
This directory has all the public header files meant to be published with the SDK. Both internal projects and samples should have this in the include path and reference the files using `#include <file>`.
