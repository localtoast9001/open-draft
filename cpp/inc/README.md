# Include Directory

This is the root source directory for header files that are shared between projects in the ../src directory.

Public headers that are published as part of any SDKs are under the pub/ sub-directory. Any other files or directories are assumed to be private.

