# C/C++ Reference Implementation

This is a C/C++ reference implementation of the core library, language library, interpreter, and various tools.  

Shared header files are in the inc/ directory, and all source and build files are under projects in the src/ directory. Test source is under the test/ directory.
